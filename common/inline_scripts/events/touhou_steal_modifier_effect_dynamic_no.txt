hidden_effect = {
    if = {
        limit = { check_modifier_value = { modifier = $modifier$ value > $threshold$ } }
        touhou_steal_modifier_effect = {
            modifier = $modifier$
            value = $value$
            duration = $duration$
        }
    }
}
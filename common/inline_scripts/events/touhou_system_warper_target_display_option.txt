option = {
    name = touhou_system_warper_target_$slot$_display
    trigger = {
        check_modifier_value = {
            modifier = touhou_system_warper_attack_target_max_add
            value >= $slot$
        }
    }
    allow = {
        custom_tooltip = {
            fail_text = spth_system_selector_opened
            NOT = { owner = { has_country_flag = has_spth_system_selector_opened } }
        }
    }
    hidden_effect = { touhou_system_warper_target_setup_flags = { slot = $slot$ } }
    country_event = { id = toho.4230 }
}
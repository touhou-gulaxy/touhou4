immediate = {
    add_deposit = $deposit$
}
option = {
    name = TH_UNFORTUNATE
    default_hide_option = yes
    tooltip = { add_deposit = $deposit$ }
}
option = {
    name = lunar_capital_psi.400.a
    tooltip = { add_deposit = $deposit$ }
}
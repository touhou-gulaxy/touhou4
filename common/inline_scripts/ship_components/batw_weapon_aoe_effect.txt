on_hit = {
    hidden_effect = {
        batw_weapon_fire_aoe_damage = {
            archaeotech = $archaeotech$ size = $size$ type = $type$ cooldown = $cooldown$ size_tag = $size_tag$ tag = $tag$
            base_damage = $base_damage$ fleet_effect_color = $fe_color$ fleet_effect_size = $fe_size$ aoe_range = $aoe_range$
        }
    }
    custom_tooltip = "BATW_FIRE_AOE_FLEET_EFFECT_$type$"
    custom_tooltip_with_params = {
        description = "BATW_FIRE_AOE_FLEET_EFFECT_COOLDOWN"
        description_parameters = {
            "AOE_COOLDOWN" = $cooldown$
        }
    }
}

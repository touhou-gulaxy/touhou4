$building$ = {
    building = $building$
    available = {
        check_modifier_value = {
            modifier = planet_barrier_capacity_use
            value < modifier:planet_barrier_capacity_add
        }
    }
}
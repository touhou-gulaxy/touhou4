			izakaya_cook_result_$type$ = {
				remove_country_flag = izakaya_cook_result_$food$
				if = { limit = { has_country_flag = izakaya_cook_result_$food$_display } remove_country_flag = izakaya_cook_result_$food$_display }
				if = {
					limit = { has_country_flag = izakaya_food_use_after_cooked }
					add_modifier = { modifier = izakaya_cook_result_$food$ days = value:izakaya_food_effect_duration mult = value:izakaya_food_effect_factor }
				} else = {
					inline_script = { script = recipes/izakaya_init_variable_array.txt }
					inline_script = { script = recipes/izakaya_change_variable_array.txt food = $food$ count = 1 }
				}
			}

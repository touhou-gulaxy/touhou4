if = {
	limit = { NOT = { is_variable_set = izakaya_ingredient_$ingredient$_val } }
	set_variable = { which = izakaya_ingredient_$ingredient$_val value = 0 }
}
random = {
	chance = value:izakaya_ingredient_gain_chance|tier|$tier$|
	touhou_randomize_variable_range = {
		which = izakaya_ingredient_$ingredient$_val rounded = yes
		min = value:izakaya_ingredient_collect_min_count|tier|$tier$ max = value:izakaya_ingredient_collect_max_count|tier|$tier$|
	}
}
reroll_random = yes
if = {
	limit = { check_variable = { which = izakaya_ingredient_$ingredient$_val value > value:izakaya_ingredient_max_val } }
	set_variable = { which = izakaya_ingredient_$ingredient$_val value = value:izakaya_ingredient_max_val }
}

if = {
	limit = { check_variable = { which = izakaya_array_foods_size value < value:country_izakaya_food_storage } }
	change_variable = { which = izakaya_array_foods_count_$food$ value = $count$ }
	change_variable = { which = izakaya_array_foods_size value = $count$ }
}

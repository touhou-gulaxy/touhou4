$type$ = {
    $resource$
    trigger = {
        owner = { has_touhou_ascension_tier = { tier = $ap$ } }
        planet = {
            NOT = { check_modifier_value = { modifier = $pm$ value > 0 } }
        }
    }
}
$type$ = {
    $resource$
    trigger = {
        owner = { has_touhou_ascension_tier = { tier = $ap$ } }
        planet = {
            is_variable_set = planet_$job_type$_genso_job_produce_faith_modifier
            NOT = { check_modifier_value = { modifier = $pm$ value > 0 } }
        }
    }
    mult = planet.planet_$job_type$_genso_job_produce_faith_modifier
}

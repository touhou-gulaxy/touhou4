produces = {
    trigger = { planet = { NOT = { check_modifier_value = { modifier = $pm$ value > 0 } } } }
    $resources$
}
produces = {
    trigger = {
        planet = {
            NOT = { check_modifier_value = { modifier = $pm$ value > 0 } }
            is_variable_set = planet_strong_genso_job_produce_faith_modifier
        }
    }
    $resources$
    mult = planet.planet_strong_genso_job_produce_faith_modifier
}
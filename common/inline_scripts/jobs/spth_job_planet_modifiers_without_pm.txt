triggered_planet_modifier = {
    potential = { planet = { NOT = { check_modifier_value = { modifier = $pm$ value > 0 } } } }
    $modifiers$
}
triggered_planet_modifier = {
    potential = { planet = { is_variable_set = planet_weak_genso_job_produce_faith_modifier NOT = { check_modifier_value = { modifier = $pm$ value > 0 } } } }
    $modifiers$
    mult = planet.planet_weak_genso_job_produce_faith_modifier
}
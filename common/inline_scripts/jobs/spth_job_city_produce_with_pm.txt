produces = {
    trigger = {
        planet = {
            spth_is_gensokyo_city = yes
            check_modifier_value = { modifier = $pm$ value > 0 }
        }
    }
    $resources$
}
produces = {
    trigger = {
        planet = {
            spth_is_gensokyo_city = yes
            is_variable_set = planet_weak_genso_job_produce_faith_modifier
            check_modifier_value = { modifier = $pm$ value > 0 }
        }
    }
    $resources$
    mult = planet.planet_weak_genso_job_produce_faith_modifier
}
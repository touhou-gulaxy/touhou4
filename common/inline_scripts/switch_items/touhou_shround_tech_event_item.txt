$weight$ = {
    modifier = { factor = 0 NOT = { has_authority = auth_gensokyo } }
    modifier = { factor = 0 has_country_flag = $tech$ }
    if = {
        limit = {
            OR = {
                has_technology = $tech$
                has_tech_option = $tech$
            }
        }
        set_country_flag = $tech$
        country_event = { id = toho.8901 }
    } else = {
        country_event = { id = toho.$event$ }
    }
}

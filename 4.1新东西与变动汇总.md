### **移 动 星 系 位 置**

是的孩子们，可以移动星系位置了

```text
move_system - Moves scoped system to a position relative to the given system/planet/ship.
move_system = {
    target = <system/planet/ship>
    min_jumps = <value> 
    max_jumps = <value> 
    min_distance = <int 0-100> 
    max_distance = <int 0-100> 
    hyperlane=<yes/no> 
    reset_terra_incognita = <yes/no>        # 重置为未知星系状态LOL
}
Supported Scopes: galactic_object
```

### 新的scope

```text
owner_or_space_owner - Scopes from an object to its owner if it exists, or to the owner of the space it is in otherwise. Works on all objects visible in star system view that can have an owner
Supported Scopes: megastructure planet country ship fleet galactic_object army starbase archaeological_site spy_network debris
Output Scope: country
```

### 一些常见effect与trigger的变动

```text
num_assigned_jobs -> 支持星系域
count_used_naval_cap -> 支持ship域
<random/every/any/count>_owned_<pop_group/pop_job/workforce> -> 支持星系域
```

### 创建舰船的变动

增加了suffix参数

```text
create_ship - Creates a new ship
create_ship = {
	name = <string/random>
	design = <ship design key/target/last_created_design, or use random_existing_design>
	random_existing_design = <ship size key>
	graphical_culture = <graphical culture key> graphical_culture_fallback = <graphical culture key>
    prefix = <Y(default)/N, determines if ship name should use owner country prefix>
	suffix = <Y/N(default), determines if ship name should use numericals. If so, name will be added to the owner country's namelist>
	colonizer_species = <species, default: fleet owner founder species> age = <int> (optional)
	rarity = <common/rare/epic/exceptional, used only if ship is space fauna> (optional, fallbacks to highest owned genetic material if applicable but not set)
	growth_stage = <integer value between 1 and the amount of growth stages in the design> (optional, use the first growth stage by default)
}
Supported Scopes: fleet starbase
```

### 恐慌状态

```text
set_terrified_by - Sets which ship terrified the target ship/fleet (all ships). Clears at the end of combat.
set_terrified_by = <attacker>
Supported Scopes: ship fleet

remove_terrified - Removes terrified state on the target ship/fleet (all ships).
remove_terrified = yes
Supported Scopes: ship fleet
```

前者为设置舰船被某个舰船恐慌，后者为移除恐慌状态。

### 替换原版超主

先定义你的超主，然后使用如下的effect

```text
replace_patron - Replace a patron with another one, creating the patron relation if it does not yet exist, and setting the new patron to the replaced patron's position in the shroud plane.
replace_patron = { new_patron = <patron_key> replaced_patron = <patron_key> }
Supported Scopes: country

remove_patron - Remove a patron from the shroud plane.
remove_patron = <patron_key>
Supported Scopes: country
```

### 灵能光环相关操作

#### effects

```text
spawn_psionic_aura - Spawns a Psionic Aura in scoped system, where country is the aura's owner.
spawn_psionic_aura = { country = <country>
Supported Scopes: galactic_object

set_aura_intensity - Sets given intensity to Psionic Aura in scoped system
set_aura_intensity = <int>
Supported Scopes: galactic_object

add_aura_intensity - Adds value to Psionic Aura intensity in scoped system
add_aura_intensity = <int>
Supported Scopes: galactic_object

refresh_psionic_aura_type - Forces an Empire's Psionic Aura to immediately update its type
refresh_psionic_aura_type = yes
Supported Scopes: country
```

#### triggers

```text
has_psionic_aura - Check if the scoped system is affected by any, or a specific type, Psionic Aura.
has_psionic_aura = <yes/no>
Supported Scopes: galactic_object

has_patron_aura - Check if the scoped country has a psionic aura from given patron.
has_patron_aura = the_eater_of_worlds
Supported Scopes: country

has_patron_relation - Check if the scoped country has established a relation with given patron. The contact state can be specified (completed by default).
has_patron_relation = {
	patron = <patron_key / any>
	contact (optional) = <completed (default)/in_progress/none/any>
}
Supported Scopes: country

is_aura_intensity_level - Checks the Psionic Aura's intensity level inside the scoped system.
is_aura_intensity_level = 2
Supported Scopes: galactic_object
```

#### modifiers

```text
- psionic_aura_effectiveness_mult, Category: Countries
- psionic_aura_intensity_add, Category: Countries
- psionic_aura_intensity_mult, Category: Countries
- ship_psionic_aura_speed_mult, Category: Ships
- ship_psionic_aura_fire_rate_mult, Category: Ships
```

### 代理人战争

```text
is_from_proxy_war - Checks if a war was started by a proxy war
is_from_proxy_war = yes/no
Supported Scopes: war

num_proxy_war - Checks the number of started proxy wars by the scoped country
num_proxy_war >=< <value>
Supported Scopes: country
```

### 杂七杂八的新修正

```text
- horizontal_attunement_add, Category: Countries
- vertical_attunement_add, Category: Countries
- attunement_mult, Category: Countries
- army_damage_against_psionic_mult, Category: Armies
- psionic_aura_resistance_mult, Category: Fleets, Countries, Star Systems
- damage_vs_psionic_empires_mult, Category: Ships
- evasion_vs_psionic_empires_mult, Category: Ships
- ship_damage_against_reckoning_mult, Category: Ships
- diplo_weight_ship_mult, Category: Ships                                   # 舰船外交权重
- ship_monthly_hull_damage, Category: Fleets                                # 月度无源船体伤害
- bonus_automated_workforce_mult, Category: Planets                         # 自动化岗效
- council_agenda_cooldown_mult, Category: Countries                         # 议程冷却时间
- empire_size_psionic_pops_mult, Category: Countries                        # 灵能人口帝国规模
- ship_friendly_territory_hull_add, Category: Ships
- ship_friendly_territory_hull_mult, Category: Ships
- ship_outside_friendly_territory_fire_rate_mult, Category: Ships
- job_[job]_automated_workforce_mult, Category: Planets, Job                # 字面意思
```

### 杂七杂八的trigger

```text
has_sensor_link_from - Checks if country has an active sensor link from another empire
has_sensor_link_from = <target>
Supported Scopes: country

has_galactic_community_emissary - Checks if scoped country has an Emissary assigned to the Galactic Community
has_galactic_community_emissary = yes/no
Supported Scopes: country

has_passed_resolution - Checks if country has passed a Galactic Community resolution in last x days/months/years
has_passed_resolution = { days/months/years = <int32> }
Supported Scopes: country

fleet_integrity - Checks the fleet integrity (shield + hull points) of a fleet.
fleet_integrity > 100
Supported Scopes: fleet

num_neighbor_systems - Checks the number of neighbor systems within a specific distance
num_neighbor_systems = {
	max_distance = 1
	limit = <triggers>
}
Supported Scopes: all

total_system_workforce_with_job_tag - Checks how much workforce that has jobs with all given tags in a system
total_system_workforce_with_job_tag = { tags = { farmer trader } value = 1000 }
Supported Scopes: galactic_object

is_last_building_changed_capital - Checks if the last building queued/dequeued/built/demolished/upgraded is a capital building. If there is no last building set, returns false.
is_last_building_changed_capital = yes
Supported Scopes: planet

category_last_picked_tradition - Checks if the last picked tradition has the given tradition category. If there is no last picked tradition set, returns false.
category_last_picked_tradition = tradition_example
Supported Scopes: country

is_researching_any_technology - Checks if the country is currently researching any technology.
is_researching_any_technology = <yes/no>
Supported Scopes: country

is_last_increased_tech_rare - Checks if the last increased tech is rare
is_last_increased_tech_rare = yes
Supported Scopes: country

is_last_increased_tech_category - Checks if the last increased tech is of the specified category:
is_last_increased_tech_category = psionics
Supported Scopes: country

count_ship_size_in_system - Checks the sum of the ship_size's size_multiplier of every ship present in the scoped system.
Checks whether the enclosed triggers return true for X/all of them
count_ship_size_in_system = {
	count = <num/all/variable>
	limit = { <triggers> }
}
Supported Scopes: galactic_object
```

### 杂七杂八的effect

```text
add_edict - Adds given edict to scoped country's government
add_edict = <key>
Supported Scopes: country

random_job_pop_group - Iterate through each pop group that contains this job - executes the enclosed effects on one of them for which the limit triggers return true. Picks the specific object randomly.
random_job_pop_group = {
	limit = { <triggers> }
	weights = { (optional - adds weights to affect the chance a specific object is selected)
		base = float
		modifier = { <add/factor = float> <triggers> }
	}
	<effects>
}
Supported Scopes: pop job

ordered_job_pop_group - Iterate through each pop group that contains this job - executes the enclosed effects on one of them for which the limit triggers return true. Picks the specific object according to the order specified (position 0, order_by = trigger:num_pops would run the effects on the X with the most pops)
ordered_job_pop_group = {
	limit = { <triggers> }
	position = <integer, starting with 0>
	order_by = <variable>/trigger:<trigger>
	inverse = yes/no (default: no - if yes, then 0 is lowest rather than highest)
	<effects>
}
Supported Scopes: pop job

every_job_pop_group - Iterate through each pop group that contains this job - executes the enclosed effects on all of them for which the limit triggers return true
every_job_pop_group = { limit = { <triggers> } <effects> }
Supported Scopes: pop job

destroy_fleet_naval_cap - Destroys up to navel_cap worth of ships in the target fleet (with death graphics)
destroy_fleet_naval_cap = {
	target=<target>
	naval_cap=<max naval cap> #default 1
	kill_leader=<yes/no> #default yes
}
Supported Scopes: all

delete_fleet_naval_cap - Deletes up to naval_cap worth of ships in the target fleet (no death graphics)
delete_ship_naval_cap = {
	target=<target>
	naval_cap=<max naval cap> #default 1
	kill_leader=<yes/no> #default yes
}
Supported Scopes: all
```

### leader_tiers

定义方式:
```pdx
leader_tier_touhou = {
    tier = 3                        # 作用等同于之前的LeaderTierNumber
    texture_postfix = "_paragon"    # 作用等同于之前的LeaderTierTexturePostfix
}
```
想要显示为“幻想的存在 指挥官”这种，仍然需要覆盖ClassPrefix与ClassPrefixColor_FR
```pdx
defined_text = {
    name = ClassPrefix
    random = no
    text = {
        trigger = {
            is_leader_tier = leader_tier_touhou
        }
        localization_key = CLASS_PREFIX_TOUHOU
    }
    text = {
        trigger = {
            is_leader_tier = leader_tier_renowned
        }
        localization_key = CLASS_PREFIX_RENOWNED
    }
    text = {
        trigger = {
            is_leader_tier = leader_tier_legendary
        }
        localization_key = CLASS_PREFIX_LEGENDARY
    }
    default = CLASS_PREFIX_NONE
}
```

用法:
```pdx
set_leader_tier = leader_tier_touhou
if = {
    limit = { is_leader_tier = leader_tier_touhou }
}
```

namespace = th_leader_trait

country_event = { # 领袖升到4级后替换mod特质为老练版本以便升级
    id = th_leader_trait.1
	hide_window = yes
	is_triggered_only = yes
	trigger = {
        has_authority = auth_gensokyo
        FROM = {
            has_skill >= 4
        }
	}
    immediate = {
        #FROM = {
        #    TH_replace_veteran_trait_effect = yes
        #}
    }
}
namespace = spth_colony

@spth_megastructure_planet_max_pop = 5
@spth_megastructure_planet_pop_minus = -5

# monthly update current barrier capacity and faith strength as planet variables (and more monthly refreshes)
country_event = {
	id = spth_colony.1
	hide_window = yes
	is_triggered_only = yes
	trigger = { has_authority = auth_gensokyo }
	immediate = {
		every_owned_colony = {
			limit = {
				OR = {
					has_modifier = spth_planet_pop_convert
					has_deposit = d_hakurei_barrier
					NOT = { carrier = { is_variable_set = spth_planet_reactor_upkeep_mult } }
				}
			}
			if = {
				limit = { count_deposit = { limit = { is_deposit_type = d_hakurei_barrier } count > 1 } }
				random_deposit = {
					limit = { is_deposit_type = d_hakurei_barrier }
					set_deposit_flag = spth_not_remove_temp
				}
				every_deposit = {
					limit = { is_deposit_type = d_hakurei_barrier NOT = { has_deposit_flag = spth_not_remove_temp } }
					remove_deposit = yes
				}
			}
			if = {
				limit = { has_deposit = d_hakurei_barrier }
				carrier = { spth_effect_refresh_barrier_and_faith = yes }
			}
			carrier = {
				if = {
					limit = { NOT = { is_variable_set = spth_planet_reactor_upkeep_mult } }
					set_variable = { which = spth_planet_reactor_upkeep_mult value = 100 }
				}
			}
			spth_count_to_be_converted_pops = yes
			if = {
				limit = { has_modifier = spth_planet_pop_convert }
				remove_modifier = spth_planet_pop_convert
				add_modifier = {
					modifier = spth_planet_pop_convert
					multiplier = spth_to_be_converted_pops_counter
				}
			}
		}
		if = {
			limit = { has_edict = spth_planet_dicisions_convert }
			every_owned_colony = {
				limit = { NOT = { has_modifier = spth_planet_pop_convert } }
				select_decision = { name = decision_gensokyo_pop_convert_activate }
			}
		}
		if = {
			limit = { has_edict = spth_planet_dicisions_sp }
			every_owned_colony = {
				if = {
					limit = { NOT = { has_deposit = d_hakurei_barrier } }
					select_decision = { name = decision_establish_barrier }
				}
			}
		}
        if = {
            limit = { has_valid_civic = civic_gensokyo }
            every_owned_colony = {
                limit = { uses_district_set = gensokyo_planet }
                carrier = {
					if = {
						limit = { NOT = { is_variable_set = touhou_planet_wrlv_var } }
						set_variable = { which = touhou_planet_wrlv_var value = 0 }
					}
					if = {
						limit = { has_modifier = touhou_gensokyo_planet_wrlv }
						remove_modifier = touhou_gensokyo_planet_wrlv
					}
					add_modifier = { modifier = touhou_gensokyo_planet_wrlv mult = touhou_planet_wrlv_var }
					if = {
						limit = { NOT = { has_deposit = d_gensokyo_planet } }
						add_deposit = d_gensokyo_planet
					}
                }
            }
        }
	}
}

# update current barrier capacity as a planet variable when the save is loaded
event = {
	id = spth_colony.2
	hide_window = yes
	is_triggered_only = yes
	immediate = {
		every_country = {
			limit = { has_authority = auth_gensokyo }
			country_event = { id = spth_colony.1 }
			if = {
				limit = { NOT = { has_country_flag = spth_dont_setting_pop_on_load } }
				country_event = { id = gensokyo_start.1145 }
			}
		}
	}
}

# monthly update current barrier capacity as a planet variable
carrier_event = {
	id = spth_colony.100
	hide_window = yes
	is_triggered_only = yes

	trigger = {
		exists = owner
		owner = { has_authority = auth_gensokyo }
		OR = {
			has_modifier = spth_planet_pop_convert
			has_deposit = d_hakurei_barrier
		}
	}
	
	immediate = {
		if = {
			limit = { has_deposit = d_hakurei_barrier }
			set_variable = {
				which = planet_genso_job_buff_modifier
				value = value:planet_genso_job_buff_modifier
			}
		}
		spth_count_to_be_converted_pops = yes
		if = {
			limit = { has_modifier = spth_planet_pop_convert }
			remove_modifier = spth_planet_pop_convert
			add_modifier = {
				modifier = spth_planet_pop_convert
				multiplier = spth_to_be_converted_pops_counter
			}
		}
	}
}
country_event = {
	id = spth_colony.202
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		has_authority = auth_gensokyo
		has_edict = spth_planet_dicisions_shrine
	}
	immediate = {
		every_owned_colony = {
			limit = {
				is_colony = yes
				OR = {
					has_building = building_system_capital
					has_building = building_ancient_palace
					has_building = building_nirvana_empire_palace
					has_building = building_hab_system_capital
                    has_building = ag_shell_world_capital_lv4
				}
				NOR = {
					has_building = building_spth_old_shirne
					has_building = building_spth_planet_shirne
				}
				# allow
				NOR = {
					has_modifier = resort_colony
					has_modifier = slave_colony
					has_building = building_imperial_capital
				}
				sapient_pop_amount >= 6000
			}
			select_decision = { name = spth_build_spth_capital_shirne }
		}
	}
}
country_event = {
	id = spth_colony.203
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		has_authority = auth_gensokyo
		has_edict = spth_planet_dicisions_th_deposits_build
	}
	immediate = {
        every_owned_colony = {
            limit = {
                has_deposit = d_hakurei_barrier
                NOT = { check_variable = { which = th_deposits_num value >= 7 } }
                OR = {
                    AND = {
                        is_artificial = no
                        NOT = { is_planet_class = pc_machine }
                        NOT = { is_planet_class = pc_hive }
                        NOT = { is_planet_class = pc_city }
                    }
                    AND = {
                        ag_is_shell_world = yes
                        check_modifier_value = { modifier = touhou_can_shellworld_use_th_deposits_build value != 0 }
                    }
                }
            }
            if = {
                limit = {
                    ag_is_shell_world = yes
                    check_modifier_value = { modifier = touhou_can_shellworld_use_th_deposits_build value != 0 }
                }
                select_decision = { name = th_deposits_build_shell_world }
            } else = {
                select_decision = { name = th_deposits_build }
            }
        }
	}
}

country_event = {
	id = spth_colony.200
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		any_owned_colony = {
			has_carrier_flag = megastructure
			has_carrier_flag = is_spth_megastructure_planet
			spth_pop_group_amount > @spth_megastructure_planet_max_pop
		}
	}
	immediate = {
		every_owned_colony = {
			limit = {
				has_carrier_flag = megastructure
				has_carrier_flag = is_spth_megastructure_planet
				spth_pop_group_amount > @spth_megastructure_planet_max_pop
				is_colony = yes
			}
			carrier_event = { id = spth_colony.201 }
		}
	}
}
carrier_event = {
	id = spth_colony.201
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		has_carrier_flag = megastructure
		has_carrier_flag = is_spth_megastructure_planet
		spth_pop_group_amount > @spth_megastructure_planet_max_pop
	}
	immediate = {
		set_variable = { which = spth_megastructure_pop_count value = 0 }
		export_trigger_value_to_variable = { trigger = spth_pop_group_amount variable = spth_megastructure_pop_count }
		change_variable = { which = spth_megastructure_pop_count value = @spth_megastructure_planet_pop_minus }
		while = {
			count = spth_megastructure_pop_count
			random_owned_pop_group = { kill_single_pop = yes }
		}
	}
}

carrier_event = {
	id = spth_colony.210
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		owner = {
			OR = {
				has_edict = spth_planet_dicisions_convert
				has_edict = spth_planet_dicisions_sp
			}
		}
	}
	immediate = {
		if = {
			limit = {
				owner = { has_edict = spth_planet_dicisions_convert }
				NOT = { has_modifier = spth_planet_pop_convert }
			}
			select_decision = {
				name = decision_gensokyo_pop_convert_activate
			}
		}
		if = {
			limit = { owner = { has_edict = spth_planet_dicisions_sp } }
			if = {
				limit = { NOT = { has_deposit = d_hakurei_barrier } }
				select_decision = {
					name = decision_establish_barrier
				}
			}
		}
	}
}

# 突触机烧外星朋友后人口转化
# A pop has finished purging
# This = Planet scope
# From = Country
# FromFrom = Pop
carrier_event = {
	id = spth_colony.220
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		from = { has_authority = auth_gensokyo }
		is_planet_class = pc_cosmogenesis_world
		check_modifier_value = { modifier = planet_convert_to_touhou value > 0 }
		fromfrom = {
			NOT = {
				pop_group_has_trait = trait_touhou
				pop_group_has_trait = trait_spth_do_not_convert
			}
		}
	}
	immediate = {
		random_list = {
			0 = {
				modifier = { add = modifier:planet_convert_to_touhou }
				from = {
					random_owned_colony = {
						limit = { NOT = { has_carrier_flag = is_spth_megastructure_planet } free_jobs > 0 }
						save_event_target_as = spth_temp_planet
					}
					if = {
						limit = { NOT = { exists = event_target:spth_temp_planet } }
						random_owned_colony = { save_event_target_as = spth_temp_planet }
					}
				}
				event_target:spth_temp_planet = { create_pop_group = { species = owner_main_species } }
				create_message = {
					type = spth_pop_convert_cosmo
					localization = spth_message_spth_pop_convert_cosmo_desc
					variable = {
						type = name
						localization = PLANET_NAME
						scope = this
					}
					variable = {
						type = name
						localization = NEW_PLANET_NAME
						scope = event_target:spth_temp_planet
					}
					days = 30
				}
			}
			100 = { modifier = { subtract = modifier:planet_convert_to_touhou } }
		}
		reroll_random = yes
	}
}

carrier_event = {
    id = spth_colony.230
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        check_modifier_value = { modifier = touhou_colony_start_num_pops_add value > 0 }
    }
    immediate = {
        create_pop_group = {
            species = owner_main_species
            size = modifier:touhou_colony_start_num_pops_add
        }
    }
}
planet_event = {
    id = spth_colony.231
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        check_modifier_value = { modifier = touhou_colony_init_progress_add value > 0 }
    }
    immediate = {
        if = {
            limit = {
                check_modifier_value = { modifier = touhou_colony_init_progress_add value < 1 }
            }
            add_colony_progress = modifier:touhou_colony_init_progress_add
        } else = {
            add_colony_progress = 1.00
        }
    }
}

country_event = {
	id = spth_colony.3
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		has_authority = auth_gensokyo
		has_country_flag = spth_pop_convert_enabled
	}
	immediate = {
		every_owned_colony = {
			limit = { has_modifier = spth_planet_pop_convert }
			carrier = {
				set_variable = { which = to_be_converted_amount value = 0 }
				if = {
					limit = { any_owned_pop_group = { has_living_standard = { type = spth_living_standards_assimilation } } }
					random_owned_pop_group = {
						limit = { has_living_standard = { type = spth_living_standards_assimilation } }
						prev = { set_variable = { which = to_be_converted_amount value = prev.trigger:pop_group_size } }
						set_pop_group_flag = spth_planet_pop_convert
					}
				}
				create_pop_group = { species = root.owner_main_species size = to_be_converted_amount }
				random_owned_pop_group = {
					limit = { has_pop_group_flag = spth_planet_pop_convert }
					spth_kill_pop_group = yes
				}
			}
		}
	}
}
country_event = {
	id = spth_colony.4
	hide_window = yes
	is_triggered_only = yes
	trigger = {
		has_authority = auth_gensokyo
		has_edict = spth_build_auto_ringworld
	}
	immediate = {
		if = {
			limit = {
				OR = {
					has_origin = origin_spth_story_gensokyo
					has_origin = origin_gensokyo_ringworld
				}
			}
			if = {
				limit = { any_system_within_border = { spth_auto_ringworld_limit = yes } }
				while = {
					count = 2 reroll_random = yes
					random_system_within_border = {
						limit = { spth_auto_ringworld_limit = yes }
						spth_auto_ringworld_build_story = yes
					}
				}
			}
		} else = {
			if = {
				limit = { any_system_within_border = { spth_auto_ringworld_limit = yes } }
				while = {
					count = 2 reroll_random = yes
					random_system_within_border = {
						limit = { spth_auto_ringworld_limit = yes }
						spth_auto_ringworld_build_common = yes
					}
				}
			}
		}
	}
}
country_event = {
    id = spth_colony.5
    hide_window = yes
    is_triggered_only = yes
    trigger = {
        has_authority = auth_gensokyo
        has_valid_civic = civic_gensokyo
    }
    immediate = {
        every_owned_colony = {
            limit = { uses_district_set = gensokyo_planet }
            carrier = {
				if = {
					limit = { NOT = { is_variable_set = touhou_planet_wrlv_var } }
					set_variable = { which = touhou_planet_wrlv_var value = 0 }
				}
				change_variable = { which = touhou_planet_wrlv_var value = value:touhou_planet_wrlv_contaminate }
				if = {
					limit = { check_variable = { which = touhou_planet_wrlv_var value < 0 } }
					set_variable = { which = touhou_planet_wrlv_var value = 0 }
				}
				if = {
					limit = { has_modifier = touhou_gensokyo_planet_wrlv }
					remove_modifier = touhou_gensokyo_planet_wrlv
				}
				add_modifier = { modifier = touhou_gensokyo_planet_wrlv mult = touhou_planet_wrlv_var }
				if = {
					limit = { NOT = { has_deposit = d_gensokyo_planet } }
					add_deposit = d_gensokyo_planet
				}
            }
        }
    }
}

carrier_event = {
	id = spth_colony.1000
	title = spth_colony.1000
	desc = spth_colony.1000.desc
	diplomatic = yes
	custom_gui = spth_event_large_window
	custom_gui_option = spth_event_large_ui_option
	picture_event_data = { room = hakuri_shrine_night_room }
	is_triggered_only = yes
	option = {
		name = spth_colony.1000.a
		hidden_effect = {
			remove_carrier_flag = spth_torifune_shrine_research_mode_soc
			remove_carrier_flag = spth_torifune_shrine_research_mode_eng
			set_carrier_flag = spth_torifune_shrine_research_mode_phy
			carrier_event = { id = spth_colony.1000 }
		}
	}
	option = {
		name = spth_colony.1000.b
		hidden_effect = {
			remove_carrier_flag = spth_torifune_shrine_research_mode_soc
			set_carrier_flag = spth_torifune_shrine_research_mode_eng
			remove_carrier_flag = spth_torifune_shrine_research_mode_phy
			carrier_event = { id = spth_colony.1000 }
		}
	}
	option = {
		name = spth_colony.1000.c
		hidden_effect = {
			set_carrier_flag = spth_torifune_shrine_research_mode_soc
			remove_carrier_flag = spth_torifune_shrine_research_mode_eng
			remove_carrier_flag = spth_torifune_shrine_research_mode_phy
			carrier_event = { id = spth_colony.1000 }
		}
	}
	option = {
		name = spth_colony.1000.exit
		default_hide_option = yes
	}
}

carrier_event = {
	id = spth_colony.1001
	hide_window = yes
	is_triggered_only = yes
	trigger = { exists = this }
	immediate = {
		if = {
			limit = {
				OR = {
					NOT = { exists = colony }
					NOT = { is_variable_set = touhou_added_planet_devastation_var }
					check_variable = { which = touhou_added_planet_devastation_var value <= 0 }
				}
			}
			set_variable = { which = touhou_added_planet_devastation_var value = 0 }
			if = {
				limit = { has_carrier_flag = touhou_add_planet_devastation_event_ongoing }
				remove_carrier_flag = touhou_add_planet_devastation_event_ongoing
			}
		} else = {
			if = {
				limit = { NOT = { has_carrier_flag = touhou_add_planet_devastation_event_ongoing } }
				set_carrier_flag = touhou_add_planet_devastation_event_ongoing
			}
			subtract_variable = { which = touhou_added_planet_devastation_var value = 1 }
			if = {
				limit = { has_modifier = touhou_added_planet_devastation }
				remove_modifier = touhou_added_planet_devastation
			}
			add_modifier = {
				modifier = touhou_added_planet_devastation
				mult = touhou_added_planet_devastation_var
				days = 1 time_multiplier = touhou_added_planet_devastation_var
			}
			carrier_event = { id = spth_colony.1001 days = 1 }
		}
	}
}

carrier_event = {
	id = spth_colony.11
	title = spth_colony.11
	desc = spth_colony.11.desc
	picture = GFX_evt_hakurei_shrine_in_night
	is_triggered_only = yes
	trigger = { NOT = { has_carrier_flag = has_open_spth_reactor_adjust_ui } }
	diplomatic = yes
	custom_gui = spiritual_power_reactor_ui
	custom_gui_option = spiritual_power_reactor_button
	picture_event_data = { room = mc_hakuri_shrine_room }
	immediate = {
		set_carrier_flag = has_open_spth_reactor_adjust_ui
		if = {
			limit = { NOT = { is_variable_set = spth_planet_reactor_upkeep_mult } }
			set_variable = { which = spth_planet_reactor_upkeep_mult value = 100 }
		}
		set_variable = { which = spth_planet_reactor_display_produce value = value:spth_planet_reactor_procude_mult }
	}
	after = {
		hidden_effect = {
			clear_variable = spth_planet_reactor_display_produce
			remove_carrier_flag = has_open_spth_reactor_adjust_ui
		}
		if = {
			limit = { has_carrier_flag = close_spth_planet_reactor_ui }
			remove_carrier_flag = close_spth_planet_reactor_ui
			owner = { remove_country_flag = has_open_gui_spth_colony_11 }
		} else = { carrier_event = { id = spth_colony.11 } }
	}
	option = {
		name = spth_colony.11.a
		custom_tooltip = spth_colony.11.tooltip
		hidden_effect = {
			remove_carrier_flag = spth_planet_reactor_use_food
			remove_carrier_flag = spth_planet_reactor_use_minerals
			remove_carrier_flag = spth_planet_reactor_use_consumer_goods
		}
	}
	option = {
		name = spth_colony.11.b
		custom_tooltip = spth_colony.11.tooltip
		hidden_effect = {
			set_carrier_flag = spth_planet_reactor_use_food
			remove_carrier_flag = spth_planet_reactor_use_minerals
			remove_carrier_flag = spth_planet_reactor_use_consumer_goods
		}
	}
	option = {
		name = spth_colony.11.c
		custom_tooltip = spth_colony.11.tooltip
		hidden_effect = {
			remove_carrier_flag = spth_planet_reactor_use_food
			set_carrier_flag = spth_planet_reactor_use_minerals
			remove_carrier_flag = spth_planet_reactor_use_consumer_goods
		}
	}
	option = {
		name = spth_colony.11.d
		custom_tooltip = spth_colony.11.tooltip
		hidden_effect = {
			remove_carrier_flag = spth_planet_reactor_use_food
			remove_carrier_flag = spth_planet_reactor_use_minerals
			set_carrier_flag = spth_planet_reactor_use_consumer_goods
		}
	}
	option = {
		name = spth_colony.11.exit
		custom_tooltip = spth_colony.11.tooltip
		default_hide_option = yes
		hidden_effect = { set_carrier_flag = close_spth_planet_reactor_ui }
	}
}
